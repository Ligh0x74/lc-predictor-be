spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lc_predictor
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

lc-predictor:
  allowed-origins: http://localhost:5173/
  jwt-key: 1024
